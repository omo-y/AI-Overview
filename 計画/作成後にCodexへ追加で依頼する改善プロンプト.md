UI改善
現在のAI Overview診断ツールのUIを改善してください。

- 総合スコアを大きな円形またはカードで表示
- 80点以上は良好、60〜79点は改善余地あり、59点以下は要改善と表示
- 項目別スコアを見やすい表にする
- 改善提案を優先度順に表示
- FAQ案とメタディスクリプション案をコピーしやすくする
- スマホ表示でも読みやすくする

既存機能は壊さないでください。2. JSONパース安定化
OllamaのJSON出力が崩れることがあるため、JSONパース処理を強化してください。

- ```json で囲まれた場合にも対応

  ```
- JSONの前後に説明文が混ざった場合にも、最初のJSON部分だけ抽出
- パース失敗時はfallbackとして空配列を返す
- エラー内容はコンソールに出すが、画面には分かりやすい日本語だけ表示

既存のルールベース診断結果は必ず表示してください。3. 診断履歴のローカル保存
診断履歴をブラウザのlocalStorageに保存できるようにしてください。

- 診断日時
- 入力本文の先頭100文字
- 総合スコア
- 評価サマリー

を保存し、画面下部に過去5件の診断履歴を表示してください。
DBは使わないでください。4. URL入力対応
本文入力に加えて、URL入力にも対応してください。

- URLが入力された場合、サーバー側でHTMLを取得
- title、meta description、h1、h2、h3、本文らしきテキストを抽出
- 抽出したテキストを診断対象にする
- URL取得に失敗した場合は分かりやすいエラーを表示
- 本文が直接入力されている場合は本文を優先

セキュリティ上、localhostやプライベートIPへのアクセスは避けるようにしてください。
